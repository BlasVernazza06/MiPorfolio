---
import "@/styles/global.css";
import { ClientRouter } from "astro:transitions";
import Header from "@/components/Navbar.astro";
import Footer from "@/components/Footer.astro";
import Aside from "@/components/Aside.astro";
import CommandPalette from "@/components/CommandPalette.jsx";
const { title, lang = "es" } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />

    <!-- Language Detection & Redirection -->
    <script is:inline>
      (function() {
        const path = window.location.pathname;
        const isRoot = path === "/" || path === "/index.html";
        
        // Sync direct navigations
        if (path.startsWith("/en")) {
          localStorage.setItem("preferred-language", "en");
          return;
        } else if (isRoot) {
          const preferredLanguage = localStorage.getItem("preferred-language");
          const systemLanguage = navigator.languages ? navigator.languages[0] : navigator.language;

          if (!preferredLanguage) {
            if (systemLanguage.toLowerCase().startsWith("en")) {
              localStorage.setItem("preferred-language", "en");
              window.location.href = "/en/";
            } else {
              localStorage.setItem("preferred-language", "es");
            }
          } else if (preferredLanguage === "en") {
            window.location.href = "/en/";
          }
        }
      })();
    </script>

    <!-- Theme Initialization Script -->
    <script is:inline>
      const getTheme = () => {
        if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
          return localStorage.getItem("theme");
        }
        return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
      };

      const applyTheme = (doc) => {
        const theme = getTheme();
        const isDark = theme === "dark";
        doc.documentElement.classList.toggle("dark", isDark);
      };

      // Inicialización inmediata
      applyTheme(document);

      // Persistencia durante la transición
      document.addEventListener("astro:before-swap", (event) => {
        // Aplicamos el tema al NUEVO documento antes de que se muestre
        applyTheme(event.newDocument);
      });
    </script>

    <!-- Background Safeguard -->
    <style is:inline>
      html.dark {
        background-color: #09090b;
      }
      html {
        background-color: #fbfcff;
      }
    </style>

    <link rel="icon" type="image/svg+xml" href="/TechIcons/icons-Web.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <ClientRouter />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="preload"
      as="style"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700;800&display=swap"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="text-zinc-900 dark:text-zinc-100 selection:bg-emerald-500/30 overflow-x-hidden"
  >
    <!-- Scroll Progress Bar -->
    <div
      id="progress-bar"
      class="fixed top-0 left-0 h-[3px] bg-gradient-to-r from-emerald-400 to-emerald-600 z-[9999] transition-all duration-300 pointer-events-none"
      style="width: 0%;"
      transition:persist
    >
    </div>

    <!-- Global Background & Immersive Effects -->
    <div
      class="fixed inset-0 z-[-1] overflow-hidden pointer-events-none"
      transition:persist
    >
      <!-- Base Color Base -->
      <div class="absolute inset-0 bg-[#fbfcff] dark:bg-[#09090b]"></div>

      <!-- Mesh Gradients for Atmosphere -->
      <div class="absolute inset-0 opacity-40 dark:opacity-60">
        <div class="absolute top-[-10%] right-[10%] w-[50%] h-[50%] bg-emerald-100 dark:bg-emerald-900/20 blur-[120px] rounded-full"></div>
        <div class="absolute bottom-[0%] left-[-5%] w-[40%] h-[40%] bg-blue-50 dark:bg-blue-900/10 blur-[100px] rounded-full"></div>
      </div>

      <!-- Noise Texture for tactile feel -->
      <div class="absolute inset-0 opacity-[0.02] dark:opacity-[0.04] mix-blend-overlay pointer-events-none" style="background-image: url('https://grainy-gradients.vercel.app/noise.svg');"></div>
    
    </div>

    <Aside lang={lang} />
    <Header lang={lang} />
    <CommandPalette client:load lang={lang} />
    <main class="relative">
      <slot />
    </main>
    <Footer lang={lang} />

    <style is:global>
      :root {
        color-scheme: light dark;
      }

      /* View Transitions: Quitamos la animación de salida */
      ::view-transition-old(root) {
        animation: none;
        display: none;
      }

      /* Opcional: Ajustamos la velocidad de entrada para que se sienta más "pro" */
      ::view-transition-new(root) {
        animation-duration: 0.4s;
        animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: "Inter", sans-serif;
        min-height: 100dvh;
        overscroll-behavior: none;
        -webkit-font-smoothing: antialiased;
      }

      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        font-family: "Outfit", sans-serif;
      }

      @media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }
      }

      #header-nav {
        animation: blur linear both 0.5s;
        animation-timeline: scroll();
        animation-range: 0 500px;
      }

      @keyframes blur {
        to {
          backdrop-filter: blur(16px);
          background-color: rgba(255, 255, 255, 0.7);
          border: 1px solid rgba(0, 0, 0, 0.05);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
        }
      }

      .dark #header-nav {
        animation: blur-dark linear both 0.5s;
        animation-timeline: scroll();
        animation-range: 0 500px;
      }

      @keyframes blur-dark {
        to {
          backdrop-filter: blur(16px);
          background-color: rgba(0, 0, 0, 0.6);
          border: 1px solid rgba(255, 255, 255, 0.1);
          box-shadow: 0 4px 30px rgba(0, 0, 0, 0.5);
        }
      }

      /* Reveal Animation: Solo opacidad para evitar saltos de layout */
      .reveal {
        opacity: 0;
        transition: opacity 0.8s cubic-bezier(0.22, 1, 0.36, 1);
      }

      .reveal.active {
        opacity: 1;
      }

      /* Custom Scrollbar */
      ::-webkit-scrollbar {
        width: 8px;
      }
      ::-webkit-scrollbar-track {
        background: transparent;
      }
      ::-webkit-scrollbar-thumb {
        background: rgba(16, 185, 129, 0.2);
        border-radius: 20px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: rgba(16, 185, 129, 0.4);
      }

      /* Background Animations */
      @keyframes blob-slow {
        0%,
        100% {
          transform: translate(0px, 0px) scale(1);
        }
        33% {
          transform: translate(30px, -50px) scale(1.1);
        }
        66% {
          transform: translate(-20px, 20px) scale(0.9);
        }
      }

      .animate-blob-slow {
        animation: blob-slow 15s infinite alternate ease-in-out;
      }

      .animation-delay-2000 {
        animation-delay: 2s;
      }

      .animation-delay-4000 {
        animation-delay: 4s;
      }
    </style>

    <script>
      let isMouseListenerActive = false;

      function init() {
        // Intersection Observer for Reveal
        const observerOptions = { threshold: 0.1 };
        const observer = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("active");
            }
          });
        }, observerOptions);

        const revealElements = document.querySelectorAll(".reveal");
        
        // Detectamos si es una navegación interna (Astro) para evitar el salto de 30px
        const isAstroNav = !!document.documentElement.getAttribute('data-astro-transition');

        // On transition, if we're going to a hash, back or it's an Astro transition, show immediately
        const isBackNav = (window as any).navigation?.activation?.navigationType === "back-forward";
        const hasHash = window.location.hash;

        revealElements.forEach((el) => {
          if (isBackNav || hasHash || isAstroNav) {
            el.classList.add("active");
          }
          observer.observe(el);
        });

        // Scroll Progress Bar
        const progressBar = document.getElementById("progress-bar");
        const updateProgress = () => {
          if (!progressBar) return;
          const winScroll = document.documentElement.scrollTop;
          const height =
            document.documentElement.scrollHeight -
            document.documentElement.clientHeight;
          const scrolled = (winScroll / height) * 100;
          progressBar.style.width = scrolled + "%";
        };

        window.addEventListener("scroll", updateProgress);

        // Cursor Effects
        if (!isMouseListenerActive) {
          document.addEventListener("mousemove", (e) => {
            const spotlight = document.getElementById("cursor-spotlight");
            const dots = document.getElementById("global-dots");
            
            if (spotlight) {
              spotlight.style.setProperty("--x", `${e.clientX}px`);
              spotlight.style.setProperty("--y", `${e.clientY}px`);
              spotlight.style.opacity = "1";
            }

            if (dots) {
              const x = (e.clientX / window.innerWidth - 0.5) * 20;
              const y = (e.clientY / window.innerHeight - 0.5) * 20;
              dots.style.transform = `translate(${x}px, ${y}px)`;
            }
          });
          isMouseListenerActive = true;
        }
      }

      // Initialize on first load and after every View Transition
      document.addEventListener("astro:page-load", init);
    </script>
  </body>
</html>
