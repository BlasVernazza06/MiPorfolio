---
import { getCollection } from "astro:content";
import TechStackCards from "./TechStackCarousel";
import { getTranslations } from "@/lib/i18n";

const { lang = "es" } = Astro.props;
const t = getTranslations(lang);

const skillsRaw = await getCollection("techStack");

const ORDER = ["Frontend", "Backend", "Herramientas", "Aprendiendo"];

const skills = skillsRaw
  .sort((a, b) => ORDER.indexOf(a.data.title) - ORDER.indexOf(b.data.title))
  .map((item) => ({
    ...item.data,
    title: t.skills.categories[item.data.title] || item.data.title
  }));
---

<div class="space-y-10">
  <header class="max-w-2xl">
    <p class="text-base md:text-lg text-muted-foreground leading-relaxed font-normal">
      {t.skills.headerPrefix}<span class="skills-highlight font-black tracking-tight uppercase">{t.skills.headerHighlight}</span>{t.skills.headerSuffix}
    </p>
  </header>

  <TechStackCards skills={skills} client:load />
</div>

<style>
  .skills-highlight {
    color: oklch(0.65 0.18 40);
  }
  :global(.dark) .skills-highlight {
    color: oklch(0.72 0.17 44);
  }
</style>
